{
  "version": "1.1",
  "last_updated": "2026-04-03",
  "description": "Known HTML/CSS/JS anti-patterns for asym-intel.info. Every Computer session reads this at Step 0. When a new error is discovered in any session, append it here before the session ends.",
  "anti_patterns": [
    {
      "id": "FE-001",
      "category": "css",
      "severity": "error",
      "description": "Inline padding-top on body instead of using base.css",
      "wrong": "<body style='padding-top:40px'>  or  <style>body{padding-top:40px}</style>",
      "correct": "base.css owns body { padding-top: 40px }. Never add inline or in a <style> block.",
      "root_cause": "Agent regenerating what base.css already provides",
      "detected_by": "validate-blueprint.py Check 2 / Check 14"
    },
    {
      "id": "FE-002",
      "category": "html",
      "severity": "error",
      "description": "nav.js loaded at bottom of <body> instead of <head>",
      "wrong": "<body>...<script src='../shared/js/nav.js'></script></body>",
      "correct": "<head>...<script src='../shared/js/nav.js'></script></head>",
      "root_cause": "Causes network bar not appearing on first paint — visible as layout flash on mobile",
      "detected_by": "validate-blueprint.py Check 13"
    },
    {
      "id": "FE-003",
      "category": "css",
      "severity": "error",
      "description": "Raw --monitor-accent used on light surface without color-mix()",
      "wrong": "color: var(--monitor-accent)",
      "correct": "color: color-mix(in srgb, var(--monitor-accent) 65%, #000)",
      "root_cause": "Fails WCAG AA contrast ratio on white for 5 of 7 monitors (WDM/GMM/FCW/ESA/ERM)",
      "detected_by": "validate-blueprint.py Check 16 (WARN)"
    },
    {
      "id": "FE-004",
      "category": "css",
      "severity": "error",
      "description": "Component styles placed in monitor.css instead of base.css",
      "wrong": ".signal-block { padding: 1rem } in monitor.css",
      "correct": "monitor.css is MAX 40 lines, accent CSS custom properties only. All components live in base.css.",
      "root_cause": "Agent writing scoped styles rather than consulting base.css — creates 7x maintenance burden",
      "detected_by": "validate-blueprint.py Check 15"
    },
    {
      "id": "FE-005",
      "category": "css",
      "severity": "error",
      "description": "overflow:hidden on layout container breaking position:sticky",
      "wrong": ".monitor-layout { overflow: hidden }",
      "correct": "overflow: clip (never hidden) on body, .monitor-layout, .monitor-main",
      "root_cause": "overflow:hidden creates a scroll container that silently breaks all sticky children",
      "detected_by": "Manual audit — not yet automated"
    },
    {
      "id": "FE-006",
      "category": "css",
      "severity": "warning",
      "description": "Hardcoded font-size below --text-min token",
      "wrong": "font-size: 0.65rem  or  font-size: 10px",
      "correct": "font-size: var(--text-min)  — never go below this token (clamp(0.8125rem,...))",
      "root_cause": "Produces illegible text on mobile — not caught until viewport testing",
      "detected_by": "validate-blueprint.py Check 11"
    },
    {
      "id": "FE-007",
      "category": "html",
      "severity": "error",
      "description": "Inline <nav data-asym-network-bar> HTML written into page source",
      "wrong": "<nav data-asym-network-bar>...</nav> hardcoded in HTML",
      "correct": "nav.js injects the network bar dynamically at runtime. Never write it inline.",
      "root_cause": "Agent copying bar structure instead of letting nav.js own injection — produces duplicate bar",
      "detected_by": "validate-blueprint.py Check 9"
    },
    {
      "id": "FE-008",
      "category": "js",
      "severity": "error",
      "description": "Custom HTML-escaping function defined inline instead of using shared esc()",
      "wrong": "function escape(s){ return s.replace(/</g,'&lt;').replace(/>/g,'&gt;') }",
      "correct": "Use esc(str) or escHtml(str) from renderer.js — already imported on every monitor page",
      "root_cause": "Agent unaware of shared renderer utilities — creates divergent escaping logic",
      "detected_by": "Code review only"
    },
    {
      "id": "FE-009",
      "category": "css",
      "severity": "error",
      "description": "background property set on .signal-block in monitor.css",
      "wrong": ".signal-block { background: var(--monitor-accent) } in monitor.css",
      "correct": "base.css owns .signal-block background with !important. monitor.css may only set border, border-radius, font-family.",
      "root_cause": "Override silently defeated by !important in base.css — causing confusion and no visual effect",
      "real_world_case": "ESA monitor.css had linear-gradient override; SCEM had rgba(0.06) override — both defeated silently",
      "detected_by": "Session audit 2026-04-01"
    },
    {
      "id": "FE-010",
      "category": "html",
      "severity": "error",
      "description": "Chart.js CDN loaded after charts.js in <head>",
      "wrong": "<script src='../shared/js/charts.js'></script><script src='chart.umd.min.js'>",
      "correct": "Chart.js CDN MUST appear before charts.js in <head>",
      "root_cause": "charts.js depends on Chart being defined at parse time — wrong order causes silent failure",
      "detected_by": "validate-blueprint.py Check 17"
    },
    {
      "id": "FE-011",
      "category": "js",
      "severity": "error",
      "description": "innerHTML set to return value of void function (produces literal 'undefined')",
      "wrong": "element.innerHTML = AsymPersistent.renderEntityList(items)  when function returns void",
      "correct": "Check whether the renderer function returns a string or writes to DOM directly. Call accordingly.",
      "root_cause": "AGM persistent.html: M07, M15 sections all set innerHTML to undefined — rendered as literal string",
      "real_world_case": "AGM persistent.html M07 risk vectors, M15 AISI pipeline, M15 lab postures — fixed PR #20 2026-04-01",
      "detected_by": "Domain audit 2026-04-01"
    },
    {
      "id": "FE-012",
      "category": "js",
      "severity": "error",
      "description": "Variable defined in one IIFE/script block referenced from another",
      "wrong": "Script block 1: (function(){ var KEYS = [...] })()  —  Script block 2: KEYS.forEach(...)",
      "correct": "Variables used across script blocks must be defined in outer scope or re-declared in each block",
      "root_cause": "SCEM dashboard: INDICATOR_KEYS defined inside IIFE in block 1; renderEscalationGauge() in block 2 referenced it — ReferenceError",
      "real_world_case": "SCEM dashboard.html INDICATOR_KEYS scope error — fixed session 2026-04-01",
      "detected_by": "Browser console error"
    },
    {
      "id": "FE-013",
      "category": "data",
      "severity": "error",
      "description": "Cron prompt update triggers off-schedule publish (guards bypassed)",
      "wrong": "Cron agent runs full pipeline when prompt file is updated mid-week",
      "correct": "Publish guard (day + hour + recency) must be the FIRST executed block. Hard exit if any condition fails. Explicitly state: 'A prompt reload is NOT a reason to publish.'",
      "root_cause": "WDM/GMM/FCW/ESA/AGM all published mid-week on 2026-04-01 after prompt updates. Recency guard was present but not enforced as first action.",
      "real_world_case": "5 monitors published out-of-schedule on 2026-04-01 — hardened guard applied to all 7 prompts",
      "detected_by": "User report 2026-04-01"
    },
    {
      "id": "FE-014",
      "category": "data",
      "severity": "error",
      "description": "JSON output truncated silently — deep sections never written",
      "wrong": "Cron agent produces one commit covering all sections — deep sections dropped when output is too long",
      "correct": "Use two-pass commit pattern: Pass 1 = core sections, Pass 2 = deep sections via read-modify-PUT. Verify all keys present before Step 3.",
      "root_cause": "WDM Category B sections (electoral_watch, digital_civil, etc.) missing from all 3 issues — cron hit output limit",
      "real_world_case": "WDM Issues 1-3 missing 8 Category B sections. Two-pass rule applied to all 7 monitors 2026-04-01.",
      "detected_by": "Session audit 2026-04-01"
    },
    {
      "id": "FE-015",
      "category": "data",
      "severity": "warning",
      "description": "source_url field present but always empty string",
      "wrong": "\"source_url\": \"\"",
      "correct": "Every heatmap entry, indicator, and intelligence item must have a non-empty source_url pointing to a Tier 1 or Tier 2 primary source",
      "root_cause": "WDM heatmap: all 29 entries had source_url = '' across Issues 1-3. Credibility gap for researchers.",
      "detected_by": "Domain audit 2026-04-01"
    },
    {
      "id": "FE-016",
      "category": "ux",
      "severity": "warning",
      "description": "Country name displayed without flag emoji where AsymRenderer.flag() is available",
      "wrong": "escHtml(actor)  →  'RU'",
      "correct": "(AsymRenderer.flag(actor) ? AsymRenderer.flag(actor) + '\\u00a0' : '') + escHtml(actor)  →  '🇷🇺 RU'",
      "root_cause": "AsymRenderer.flag(iso2) is available on every monitor page via renderer.js. Always use it wherever a country name or actor code appears in a table, chart label, card, or timeline.",
      "applies_to": "All monitor pages — tables, charts, timelines, cards, delta strips, actor boards. Wherever a country name or ISO-2 actor code appears.",
      "real_world_case": "FCW campaign timeline actor group labels showed 'RU', 'CN' etc with no flags. Actor profiles (same page) already used AsymRenderer.flag correctly — inconsistency.",
      "detected_by": "User review 2026-04-01"
    },
    {
      "id": "FE-017",
      "category": "css",
      "severity": "error",
      "description": "Raw --monitor-accent used as text colour on tinted panel background (monitor-accent-bg)",
      "wrong": "style=\"background:var(--monitor-accent-bg);color:var(--monitor-accent)\"",
      "correct": "style=\"background:var(--monitor-accent-bg);color:color-mix(in srgb,var(--monitor-accent) 65%,#000)\"",
      "root_cause": "var(--monitor-accent-bg) is a light tinted surface (~12% opacity). Raw --monitor-accent fails WCAG AA contrast on it for 5 of 7 monitors. The 65% darkened mix gives minimum 4.8:1 contrast.",
      "applies_to": "All JS-generated elements with tinted backgrounds: badges, risk-heat cells, tag chips, timeline labels, actor boards, delta strip items, campaign cards.",
      "scope_note": "This violation occurs almost entirely in JavaScript-generated innerHTML strings — not detectable by the static validator (Check 20). Must be caught by code review and anti-patterns awareness.",
      "detected_by": "COMPUTER.md CONTRAST RULES + code review"
    },
    {
      "id": "FE-018",
      "category": "ux",
      "severity": "warning",
      "description": "Signal block panel does not link to the primary source story",
      "wrong": "Signal block shows headline and summary with no link",
      "correct": "Always render a 'Read the top story ↓' link in every signal block where source_url is available. Pattern: (sourceUrl ? '<div style=\"margin-top:var(--space-3)\"><a class=\"source-link\" href=\"' + escHtml(sourceUrl) + '\" target=\"_blank\" rel=\"noopener\">Read the top story ↓</a></div>' : '')",
      "link_text": "Read the top story ↓",
      "applies_to": "All monitor dashboard signal/lead-signal blocks. Every signal block that references a specific story or development must link to it. The source_url is always present in report-latest.json signal object.",
      "rationale": "Users reading 'Confirmed Lead Signal: Hungary 11 days...' have no way to follow up without navigating manually. The source_url is collected every issue — surfacing it is a one-line fix.",
      "real_world_case": "FCW, WDM dashboards had no source link on signal block. ESA had 'Source →'. SCEM had 'Primary source →'. Standardised to 'Read the top story ↓' across all 7 monitors.",
      "detected_by": "User review 2026-04-01"
    },
    {
      "id": "FE-019",
      "category": "content",
      "description": "Hugo brief filename must match front-matter date. Filename slug determines the public URL — a filename of 2026-04-02-weekly-brief.md produces a future-dated URL even if the front matter says date: 2026-04-01. Always set filename = YYYY-MM-DD matching the date: field in front matter exactly.",
      "example_bad": "Filename: 2026-04-02-weekly-brief.md with front matter date: 2026-04-01T09:00:00Z",
      "example_good": "Filename: 2026-04-01-weekly-brief.md with front matter date: 2026-04-01T09:00:00Z",
      "why": "Future-dated URLs appear in sitemap.xml and can cause Google Search Console to reject the sitemap as containing invalid entries. Dates must be consistent.",
      "applies_to": "all cron agents writing Hugo briefs",
      "first_seen": "2026-04-01"
    },
    {
      "id": "FE-020",
      "category": "js",
      "severity": "error",
      "description": "Plain text escaping (escHtml/innerHTML with escHtml) applied to content fields that contain markdown or HTML — produces literal ** and <a> text in the rendered page",
      "wrong": "briefEl.innerHTML = '<p>' + escHtml(d.weekly_brief) + '</p>'  →  renders '**bold**' and '<a href=...' literally",
      "correct": "Use the safe renderer that splits on <a>...</a> tags (pass through), escapes only the text nodes, then applies **bold** → <strong> transform. Never run escHtml() over a field that may contain markdown or HTML markup. Use the inline 3-line renderMarkdown from report.html — NOT AsymRenderer.renderMarkdown (bug with **bold** adjacent to <a> tags).",
      "root_cause": "weekly_brief and similar narrative fields are generated by cron agents using markdown syntax (**bold**, numbered lists) and raw HTML anchor tags. Treating them as plain text destroys the formatting.",
      "applies_to": "Any data field that may contain markdown or HTML: weekly_brief, note, summary fields generated by LLMs, narrative sections. Check the actual data content before choosing renderer.",
      "real_world_case": "WDM dashboard weekly_brief section showed '**1. HUNGARY — 11 DAYS...**' literally instead of bold heading. Fixed with safe renderer that splits on <a> tags and applies bold transform. 2026-04-02.",
      "safe_renderer_pattern": "Split text on (<a\\s[^>]+>.*?</a>) — odd-indexed parts are anchor tags (pass through), even-indexed parts are text nodes (escHtml + markdown transforms). Use inline pattern from report.html, NOT AsymRenderer.renderMarkdown.",
      "detected_by": "User review 2026-04-02"
    },
    {
      "id": "FE-021",
      "category": "html",
      "severity": "error",
      "description": "New HTML section injected outside </main> closing tag",
      "wrong": "</main><section id='cross-monitor'>...</section>",
      "correct": "All new sections must be placed inside <main>. Insert before the </main> closing tag.",
      "root_cause": "Agent appending new sections after </main> — content renders outside the layout container, causing width/padding/theme issues.",
      "applies_to": "All monitor HTML pages when adding new dashboard sections.",
      "detected_by": "Sprint 3 Groups B+C audit 2026-04-02"
    },
    {
      "id": "FE-022",
      "category": "js",
      "severity": "error",
      "description": "Render call injected after .catch() block instead of inside the .then() chain",
      "wrong": "fetch(url).then(r=>r.json()).then(d=>{ ... }).catch(e=>{ ... }); renderCrossMonitor(d);",
      "correct": "renderCrossMonitor(d) must be called inside the .then() handler, not after the .catch() block. Data is not in scope outside the promise chain.",
      "root_cause": "Variable 'd' is not in scope outside the .then() callback — causes ReferenceError or renders with stale/undefined data.",
      "applies_to": "All monitor dashboard pages with fetch+render patterns.",
      "detected_by": "Sprint 3 Groups B+C audit 2026-04-02"
    },
    {
      "id": "FE-023",
      "category": "html",
      "severity": "warning",
      "description": "Sidebar nav link omitted when adding a new dashboard section",
      "wrong": "New #cross-monitor section added to dashboard.html but no sidebar anchor link added",
      "correct": "Every new named section must have a corresponding <a href='#section-id'> in the sidebar nav. Update ALL pages for that monitor.",
      "root_cause": "Agent adds the section content but forgets to wire the sidebar navigation link — section is unreachable from nav.",
      "applies_to": "All monitor pages with sidebar navigation.",
      "detected_by": "Sprint 3 Groups B+C audit 2026-04-02"
    },
    {
      "id": "FE-024",
      "category": "js",
      "severity": "error",
      "description": "escHtml() called in outer scope before DOMContentLoaded — ReferenceError",
      "wrong": "const label = escHtml(someValue);  // at top of <script> block, outside DOMContentLoaded",
      "correct": "All calls to escHtml(), AsymRenderer methods, and DOM manipulation must be inside DOMContentLoaded or a fetch .then() callback.",
      "root_cause": "escHtml is defined in renderer.js which loads asynchronously. Calling it before the DOM is ready causes ReferenceError.",
      "applies_to": "All monitor pages — any <script> block that uses renderer.js utilities.",
      "detected_by": "Sprint 3 Groups B+C audit 2026-04-02"
    },
    {
      "id": "FE-025",
      "category": "data",
      "severity": "error",
      "description": "Empty file produced by bash associative array base64 encoding in GitHub Actions / shell scripts",
      "wrong": "declare -A map; map[key]='value'; echo \"${map[key]}\" | base64  →  produces empty or corrupted output",
      "correct": "Avoid bash associative arrays for multi-key data in shell scripts. Use Python, jq, or simple sequential variables instead. Verify non-empty output before committing.",
      "root_cause": "Bash associative arrays behave inconsistently across shells and environments (especially GitHub Actions runners). Base64 encoding of associative array output produces empty strings silently.",
      "applies_to": "GitHub Actions workflows and any shell scripts that write to repo files.",
      "detected_by": "Sprint 3 Groups B+C audit 2026-04-02"
    },
    {
      "id": "FE-026",
      "category": "hugo-template",
      "severity": "error",
      "description": "Hugo --minify double-escapes content inside <script> tags, breaking JSON-LD structured data",
      "wrong": "<script type=\"application/ld+json\">{{ $dict | jsonify | safeHTML }}</script>  →  produces {\"@type\":\"NewsArticle\"} with escaped quotes — invalid JSON-LD",
      "correct": "{{ printf `<script type=\"application/ld+json\">%s</script>` ($dict | jsonify) | safeHTML }}  →  wraps entire tag in safeHTML, preventing minifier from touching the JSON content",
      "root_cause": "Hugo minifier (tdewolff/minify) processes <script> tag content as HTML and escapes double-quote characters. Applying safeHTML to the jsonify output alone does not prevent this — the minifier runs after template rendering. Wrapping the entire <script> tag in printf+safeHTML marks the whole element as trusted HTML, bypassing minification.",
      "applies_to": "All Hugo templates that output JSON-LD, especially layouts/partials/head.html and layouts/_default/single.html. Also applies to any structured data (Schema.org, JSON config) rendered inside <script> tags via Hugo templates.",
      "detected_by": "Session 2026-04-03 — JSON-LD structured data implementation"
    }
  ]
}
